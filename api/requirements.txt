fastapi==0.115.0
uvicorn[standard]==0.32.0
psycopg[binary]==3.2.3

# Auth (Phase 1)
argon2-cffi==23.1.0
python-jose[cryptography]==3.3.0

# R2 / S3 client (Phase 2b)
# Cloudflare R2 is S3-API compatible, so we use boto3 with a
# custom endpoint URL. Same library AWS S3 uses.
boto3==1.35.20

# Test deps
pytest==8.3.3
httpx==0.27.2
