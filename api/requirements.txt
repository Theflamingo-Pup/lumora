fastapi==0.115.0
uvicorn[standard]==0.32.0
psycopg[binary]==3.2.3

# Test-only dependencies. We include them here for simplicity,
# but a more polished project would split them into requirements-dev.txt.
pytest==8.3.3
httpx==0.27.2
